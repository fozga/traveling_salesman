    use std::vec::Vec;

    pub type ValueType = i32;
    pub type IndexType = usize;
    pub type RowType = Vec<ValueType>;
    pub type GraphType = Vec<RowType>;
    pub type PathType = Vec<IndexType>;
